# 风控矩阵审核器 v1.9.19

当前程序及默认激活规则为1.9.19。本版新增第二批8份省公司模板，使用全局唯一`business_id`区分重复的业务展示编号；`01`、`02`、`03`、`08`可在不同业务中重复，`28`至`31`按业务名称正常识别。程序仍将问题写入各业务工作表的“审核意见”列，并由独立代码在输出目录根部生成两列版`审核统计表.xlsx`；该统计表不属于审核规则。`run_audit.py`优先使用包内源码，并配置LibreOffice位置。

业务材料支持 `.xlsx`、`.xls` 和 `.et`；说明或申请材料支持 `.docx`、`.doc` 和 `.pdf`。程序按 `.et` 的真实内部格式读取 OOXML 或 BIFF 内容，审核副本统一输出为 `.xlsx`，原始 `.et` 不修改；无法识别的 WPS 专有结构会明确报告不能解析。

## 包内内容

- `审核器/src`：完整源码，可通过`python -m pip install ./审核器`安装。
- `审核器/rulepacks`：当前规则及历史规则、草稿；默认只启用1.9.19。历史版本不代表当前口径。
- `审核器/开发蓝图.md`、`规则配置模块设计.md`、`程序判定能力优化蓝图.md`、`程序能力修复说明-v1.7.0.md`等开发文档。
- `审核器/tests`：完整测试源码；`delivery_test.py`提供不依赖原始报送材料的测试入口。
- `审核/`：当前规则引用的第一批11份、第二批8份省公司基准矩阵及会计主体清单。基准保持原相对目录及原哈希。
- `config_examples/`：历史手动范围示例，正常审核无需填写或使用。
- `审核器/models`：已有本地Embedding模型、来源说明和许可证，随包保留；当前规则默认关闭它，审核不调用大模型或在线推理。
- `outputs/程序能力修复-v1.7.0`：本轮回测报告及选择的定位证据，其中含原文摘录和单位名称。未打入各单位原始报送Excel、历史审核副本、开发缓存、虚拟环境。

旧开发文档中的历史输出及原报送包链接未全部随包提供；本轮回测报告链接对应的证据已纳入。历史证据中的原电脑路径作为来源记录保留，不作为跨电脑运行依赖。仅安装wheel并不包含规则、基准和模型，须保留整个解压目录。

## 安装

已有环境可安装新版：`python -m pip install -r 审核器/requirements.lock`，然后`python -m pip install --no-deps --force-reinstall ./审核器`。PDF识别使用pypdf依赖，审核不需联网。

建议使用Python 3.12（程序声明支持3.11及以上，当前验证使用3.12）及已安装的LibreOffice。Python和LibreOffice安装程序、第三方Python依赖安装包不包含在本ZIP中。首次安装依赖需要访问配置的软件包镜像；审核阶段不需要联网。

先解压，再进入解压目录。Windows PowerShell执行：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r 审核器/requirements.lock
.\.venv\Scripts\python.exe -m pip install --no-deps .\审核器
.\.venv\Scripts\python.exe run_audit.py --doctor
```

macOS/Linux执行：

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -r 审核器/requirements.lock
.venv/bin/python -m pip install --no-deps ./审核器
.venv/bin/python run_audit.py --doctor
```

启动入口优先使用`--soffice`或`RISK_AUDIT_SOFFICE`设置，也查找PATH及常见安装目录。例如Windows可设置：

```powershell
$env:RISK_AUDIT_SOFFICE = 'C:\Program Files\LibreOffice\program\soffice.exe'
.\.venv\Scripts\python.exe run_audit.py --doctor
```

macOS可使用：

```bash
export RISK_AUDIT_SOFFICE='/Applications/LibreOffice.app/Contents/MacOS/soffice'
.venv/bin/python run_audit.py --doctor
```

doctor检查配置、基准哈希及转换程序位置，不等于实际转换成功。当前审核核心的XLS转换曾在macOS验证；Windows/Linux的转换、临时目录URI及Office保真需要技术人员在目标电脑用真实XLS验收，不能仅凭路径识别成功认定兼容。省公司基准中也包含XLS，所以审核XLSX报送材料时仍需转换程序。

Embedding保持关闭时不需要安装语义依赖。确有需要研究本地候选能力时，再安装`审核器/requirements-semantic.lock`，按照`审核器/本地语义辅助运行说明.md`操作；不要直接改写冻结规则包。

## 审核

以下命令以macOS/Linux写法示例，Windows将`.venv/bin/python`换成`.\.venv\Scripts\python.exe`。相对路径以解压目录为基准，输入材料建议使用绝对路径。

```bash
# 只读试跑，输入为实际报送目录
.venv/bin/python run_audit.py trial --input '/实际路径/待审核材料' --project-root . --runs-root runs

# 生成审核副本，在识别的正式业务表末尾写入程序审核意见
.venv/bin/python run_audit.py audit --input '/实际路径/待审核材料' --output '审核输出/本次结果' --project-root . --runs-root runs
```

### 规则启停配置

项目根目录的 `audit-config.json` 用于设置默认停用的规则展示编号。当前默认停用规则三：

```json
{
  "disabled_rules": ["R03"]
}
```

同一展示编号下的全部子规则会同时停用，因此默认运行不再生成【第3条】提示。某次审核需要临时启用规则三时，增加 `--enable-rule R03`：

```bash
.venv/bin/python run_audit.py audit \
  --enable-rule R03 \
  --input '/实际路径/待审核材料' \
  --output '审核输出/本次结果'
```

命令行启用只影响本次运行，不修改 `audit-config.json`。需要使用其他配置文件时，通过 `--config <文件路径>` 指定。不存在的规则编号或无法启用的规则会在审核前报错。本次最终生效的启停选择记录在 `runs/<run-id>/snapshot.json` 的 `runtime_rule_selection` 中。

原文件不改写。每次使用新的输出目录；默认隐藏工作表不参与审核。人工对照需要算法结束后另行运行，详见`审核器/README.md`。本包不是双击即可运行的独立exe，也未包含此前人工／程序两列对照的Excel成品。

程序先扫描整批材料并校验会计主体，匹配失败或证据冲突时记录错误并终止。通过后记录主体名单、统一加载一次公共基准，再按“主体＋业务＋矩阵类型”逐个处理。每个业务的矩阵和三清单一起解析，仅在本业务内审核并立即输出，然后处理下一个业务。批次汇总在全部业务结束后生成；后续业务异常时，已经生成的副本和业务结果会保留。

审核进度、文件读取明细、告警、错误和异常堆栈统一保存在`runs/<run-id>/audit.log`，不写入终端；最终 JSON 摘要仍走标准输出流。运行目录建立前的版本、LibreOffice 路径等启动错误仍由`run_audit.py`写入标准错误流，此时不会生成`audit.log`。主体结果保存在`runs/<run-id>/entities/0001/`，各业务的结果、意见、检查状态、限制及输出告警保存在其`businesses/0001/`等顺序编号目录。主体`result.json`包含已处理业务数量和`write_completed`；业务或说明输出尚未全部结束时，主体不会标记完成。试跑同样记录业务结果及日志，但不生成审核副本。批次摘要的`entity_results`、`business_results`和`log_file`分别给出主体结果、业务结果和日志路径。

单个 OOXML 工作表 XML 超过 64 MiB，或工作表声明范围异常膨胀时，程序会先流式检查真实内容边界。真实内容位于 IV 列以内时，仅在运行目录的临时副本中删除边界外的纯样式空单元格、空行和远端空白合并区域后继续审核，真实业务区合并和原件不变，净化开始和完成会写入`audit.log`；极远列存在真实内容时不猜测删除，文件会明确记录解析失败并提示人工确认工作表范围。

审核范围直接从本次上传材料确定，无需另写 JSON 配置：有“风控矩阵应用清单”或“适用性匹配统计表”时，读取第一批的适用声明并与上传矩阵核对；没有附件时，实际上传的业务就是本次适用范围。未上传且附件未声明适用的业务不判漏报。“部分适用”的矩阵仍须逐措施审核。

声明适用却未找到矩阵、声明不适用却上传、附件互相矛盾或无法读取，均在`本次审核范围.md`和`本次审核范围.json`集中提示；审核模式的辅助报告统一保存在`runs/<run-id>/_risk_audit`目录，试跑模式保存在运行根目录。单位代码无法唯一识别时，`主体代码待确认.md`和`entity_alerts.json`也遵循同一目录规则。上层共用附件按明确的单位名称或“区县（除……）”对象分配，无法确定对象时提示核对。

`--scope-file`仅保留作为可选的手动范围覆盖入口，普通报送审核不需要使用。手动覆盖和原始附件的核对提示仍会保留。

审核副本在识别到的正式业务表末尾添加或复用“审核意见”列，历史初审意见保留。
写入模式完成后还会在输出目录根部生成 `审核统计表.xlsx`。表中仅有“单位”和“备注”两列；单位保留审核包名到业务目录上一级的完整路径，备注按业务目录分段，并按规则序号组合统计矩阵和三清单的审核意见数量。该文件即使存在未审核文件、`write_completed=false` 也会汇总已完成的审核意见。

### 审核完成后删除程序输出列

确认审核结果无问题后，可以将审核目录中的程序输出列清理到一个新目录：

```bash
.venv/bin/python clean_audit_columns.py \
  --input '/实际路径/审核结果' \
  --output '/实际路径/清理后结果'
```

Windows PowerShell 将 `.venv/bin/python` 换成 `.\.venv\Scripts\python.exe`。脚本仅删除列名为“审核意见”、“省公司版本责任主体（核对后删除）”和“岗位清单已有的控制措施编号”的列，不删除“历史审核意见”等其他列。目录层级和非 Excel 文件原样保留；未命中目标列的 Excel 文件也按原字节复制。点号或 `~$` 开头的 Office/WPS 临时文件会被忽略，不读取也不复制到结果目录。

为防止覆盖审核结果，输出目录必须不存在，且不得与输入目录相同或相互包含。脚本先写入临时目录，所有文件成功后才生成最终输出；中途失败不会留下半成品输出目录。执行完成后，终端会输出处理的工作簿数、原样复制的文件数和删除列数。

未识别到业务表的文件也会原样保留在输出目录，但不会添加意见列；名单保存在
`runs/<run-id>/_risk_audit/未审核文件.json`，运行摘要的`unparsed_files`和`unparsed_file_paths`列出数量与路径。
存在未审核文件时`write_completed`为false、`has_check_limits`为true，不能将文件已复制视为审核完成。
XLS转换后的目标名称与其他副本冲突时，程序会在写入前拒绝，避免覆盖原样保留的材料。

## 验证与开发

```bash
.venv/bin/python run_audit.py config-validate 审核器/rulepacks/releases/1.9.16
.venv/bin/python delivery_test.py
```

完整工作区的v1.7.0曾通过623项测试。交付测试会明确排除7项依赖未随包提供的原始材料、历史输出或原工作区独立验收脚本的集成入口，具体名称及原因由测试入口打印。未安装NumPy时不收集`test_semantic_v150.py`中的42项可选组件测试；编码器集成测试未安装可选依赖时显示skip。需要执行包内语义组件测试，请先安装`审核器/requirements-semantic.lock`。未收集、排除或跳过均不计作通过。`交付验证.json`记录原始v1.7.0交付；新版验证见`outputs/审核确认稿V1-v1.8.0/验证报告.md`。

新版可重复运行的构造资料放在`测试材料/审核确认稿V1`，运行命令见`测试材料/说明.md`。样例手动范围仅用于程序测试，真实材料默认自动读取附件和上传业务确定范围。

开发时可在已有依赖环境安装`pip install -e 审核器`，然后执行对应测试。新规则从草稿发布，冻结的`releases/1.7.0`不可原地修改。新版本不要直接重跑发布脚本覆盖；规则版本不可变。默认版本记录位于`审核器/rulepacks/active.json`。

`文件校验清单.json`记录包内文件SHA-256；ZIP旁边的同名`.sha256`记录整个压缩包校验值。包内模型的来源与许可证在模型目录中保留。
