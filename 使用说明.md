# 风控矩阵审核器使用说明

风控矩阵审核器 2.0 是完全单机离线的本机 Web 应用，支持 Windows 10/11 x64 和 Apple Silicon macOS。页面由当前电脑上的服务提供，只监听 `127.0.0.1`；材料不会上传，运行时不需要互联网。

## 一 选择发布包

- Windows 10/11 x64：`风控矩阵审核器-v2.0.0-Windows-x64.zip`
- Apple Silicon macOS：`风控矩阵审核器-v2.0.0-macOS-arm64.zip`

将 ZIP 完整解压到普通文件夹。移动程序时应整体移动解压目录，不要只复制启动脚本。

## 二 启动审核器

### Windows

双击 `启动审核器.bat`。Windows 会打开终端并启动本机服务，随后用默认浏览器打开任务中心。

### macOS

双击 `启动审核器.command`。macOS 会打开终端并启动本机服务，随后用默认浏览器打开任务中心。首次打开若被 Gatekeeper 拦截，请参阅 `risk-audit/macOS Apple Silicon Web版使用说明.md`。

运行期间必须保持终端窗口打开。关闭浏览器不会停止审核器或正在运行的任务；再次双击当前平台的启动脚本，可重新打开已有页面，不会启动第二个服务。

## 三 创建任务

1. 点击“选择目录”，选择待审核材料所在的输入目录。
2. 检查任务名称和页面显示的输出预览。
3. 点击“创建并开始审核”。

用户只选择输入目录。输出位置固定为解压目录下的 `outputs`，不能从页面另选。每个任务使用独立子目录，不会覆盖以前的结果。

## 四 查看进度和结果

任务中心显示排队、运行、完成、失败或取消状态。任务完成后，可在详情页打开输出目录、审核统计表和集中复核结果。原始输入材料不会被改写。

## 五 退出和强制结束

正常退出时，点击页面左下角的“退出审核器”。如果仍有任务运行，页面会请求安全停止，然后关闭本机服务。

关闭或终止终端窗口会强制结束审核器及当前 Worker 和 LibreOffice 子进程。正在执行的任务可能不完整，下次启动时会标记为意外中断。仅在页面无法使用或必须立即结束时采用此方式。

## 六 离线运行边界

发布包已包含 Python 运行环境和审核依赖、HTML/CSS/JavaScript 页面、Bootstrap 与图标字体、审核规则与基准、会计主体清单和 LibreOffice。目标电脑不需要预装 Python、Node.js、Qt 或 LibreOffice。

发布清单和 Web 供应商清单用于检测缺失或被修改的运行文件。任务结果只能通过任务记录打开，页面参数不能访问电脑上的其他路径。

## 七 常见问题

### 页面提示运行资源缺失

重新完整解压对应平台的 ZIP，不要从其他版本混合复制 `app` 或 `runtime`。

### 任务失败

先在任务详情查看错误，再检查 `data/logs` 中的日志。输入目录必须存在、可读，且不能位于本程序的 `outputs` 中。

### 浏览器显示无法连接

确认启动脚本打开的终端仍在运行。如果终端已结束，请重新双击启动脚本。如果安全软件拦截本机回环访问，只需允许审核器访问 `127.0.0.1`，无需开放局域网或公网端口。
